% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/natlan.R
\name{natlan}
\alias{natlan}
\title{Translate DELTA-format data into natural language descriptions via CONFOR}
\usage{
natlan(
  delta,
  heading = NULL,
  replace_angle_brackets = TRUE,
  omit_character_numbers = TRUE,
  omit_inapplicables = TRUE,
  omit_comments = TRUE,
  omit_inner_comments = TRUE,
  omit_final_comma = TRUE,
  translate_implicit_values = FALSE,
  omit_lower_for_characters = NULL,
  omit_or_for_characters = NULL,
  omit_period_for_characters = NULL,
  new_paragraphs_at_characters = NULL,
  item_subheadings = NULL,
  link_characters = NULL,
  replace_semicolon_by_comma = NULL,
  print_width = 80,
  exclude_items = NULL,
  exclude_characters = NULL,
  vocabulary = NULL,
  remove_temp = TRUE
)
}
\arguments{
\item{delta}{A Delta dataset object (created by \code{\link{load_delta}})}

\item{heading}{Heading for the descriptions (default = NULL, uses heading from specs file)}

\item{replace_angle_brackets}{Replace angle brackets with parentheses (default = TRUE)}

\item{omit_character_numbers}{Omit character numbers from descriptions (default = TRUE)}

\item{omit_inapplicables}{Omit 'or not applicable' from descriptions (default = TRUE)}

\item{omit_comments}{Omit comments in attributes (default = TRUE)}

\item{omit_inner_comments}{Omit inner comments in character list and attributes (default = TRUE)}

\item{omit_final_comma}{Omit final comma before 'and' (default = TRUE)}

\item{translate_implicit_values}{Output implicit values in descriptions (default = FALSE)}

\item{omit_lower_for_characters}{List of numeric characters to omit lower values}

\item{omit_or_for_characters}{List of characters to omit 'or' between alternatives}

\item{omit_period_for_characters}{List of characters where period would normally terminate a sentence}

\item{new_paragraphs_at_characters}{List of characters where new paragraphs should start}

\item{item_subheadings}{List of subheadings for items (delimiter/character/value)}

\item{link_characters}{List of characters specifying how attributes are combined}

\item{replace_semicolon_by_comma}{List of characters to use comma instead of semicolon}

\item{print_width}{Maximum line length for output (default = 80)}

\item{exclude_items}{List of items to exclude}

\item{exclude_characters}{List of characters to exclude}

\item{vocabulary}{List for custom vocabulary (default = NULL)}

\item{remove_temp}{Logical; if TRUE, remove temporary files after conversion (default = TRUE)}
}
\value{
Invisibly returns the descriptions as a character vector
}
\description{
This function replicates the TRANSLATE INTO NATURAL LANGUAGE command in the
DELTA system. It creates the necessary directives file and runs the CONFOR
program to generate natural language descriptions.
}
\details{
The function assumes that the CONFOR program executable is available
in the system PATH or in the current working directory.
}
\examples{
\dontrun{
# Load a DELTA dataset
delta <- load_delta("chars_viola", "items_viola", "specs_viola")

# Generate natural language descriptions
natlan(delta)

# With custom heading and subheadings
natlan(delta, 
       heading = "Flora of Region X - species descriptions",
       item_subheadings = list(
         "87" = "Transverse section of lamina",
         "96" = "Leaf epidermis"
       ))
}
}
