% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_delta.R, R/utils.R
\name{save_delta}
\alias{save_delta}
\title{Save a Delta dataset to disk}
\usage{
save_delta(delta, dir = ".", prefix = "", overwrite = FALSE, width = 79)

save_delta(delta, dir = ".", prefix = "", overwrite = FALSE, width = 79)
}
\arguments{
\item{delta}{A Delta dataset object (Rcpp_DeltaParser from load_delta())}

\item{dir}{Path to the directory where files should be saved (default: current directory)}

\item{prefix}{Optional prefix for filenames (default: "")}

\item{overwrite}{Whether to overwrite existing files (default: FALSE)}

\item{width}{Maximum line width for output files (default: 79, range: 40-132)}
}
\value{
Invisibly returns the input delta object

Invisibly returns the input delta object
}
\description{
This function saves a Delta dataset object to disk as three plain text files:
"chars", "items", and "specs" in the DELTA format.

This function saves a Delta dataset object to disk as three plain text files:
"chars", "items", and "specs" in the DELTA format.
}
\examples{
\dontrun{
delta <- load_delta("chars", "items", "specs")
save_delta(delta)
save_delta(delta, dir = "output", prefix = "my_", overwrite = TRUE)
save_delta(delta, width = 120)  # Use wider output
}
\dontrun{
delta <- load_delta("chars", "items", "specs")
save_delta(delta)
save_delta(delta, dir = "output", prefix = "my_", overwrite = TRUE)
save_delta(delta, width = 120)  # Use wider output
}
}
