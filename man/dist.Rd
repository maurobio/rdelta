% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist.R
\name{dist}
\alias{dist}
\title{Generate Distance Matrix using Gower's Coefficient}
\usage{
dist(
  delta,
  heading = NULL,
  match_overlap = FALSE,
  minimum_comparisons,
  phylip_format = FALSE,
  exclude_items = NULL,
  exclude_characters = NULL,
  remove_temp = TRUE
)
}
\arguments{
\item{delta}{A Delta dataset object (created by \code{\link{load_delta}})}

\item{heading}{Heading for the output (default = NULL, uses heading from specs file)}

\item{match_overlap}{For unordered multistate characters, the contribution
of a character to the distance between two items is 0 if the items have
any of the values of the character in common (default = FALSE)}

\item{minimum_comparisons}{Minimum number of character comparisons required
to calculate the distance between two items (default = ceiling(sqrt(number
of included characters)))}

\item{phylip_format}{Causes the taxon names to be interleaved with the rows
of the distance matrix, as required by the program Phylip (default = FALSE)}

\item{exclude_items}{A list of items to be excluded (default = NULL)}

\item{exclude_characters}{A list of characters to be excluded (default = NULL)}

\item{remove_temp}{Logical; if TRUE, remove temporary files after conversion}
}
\value{
Invisibly returns 0 on success
}
\description{
This function replicates the DIST command in the DELTA system for generating
a distance matrix using Gower's coefficient. It creates the necessary
directives files and runs the CONFOR and DIST programs.
}
\details{
The function assumes that the CONFOR and DIST program executables are available
in the system PATH or in the current working directory.
}
\examples{
\dontrun{
# Load a DELTA dataset
delta <- load_delta("chars", "items", "specs")

# Generate distance matrix
dist(delta)

# With custom options
dist(delta, match_overlap = TRUE, phylip_format = TRUE, 
     minimum_comparisons = 10)
}
}
